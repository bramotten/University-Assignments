# Rekenmachine_II WASM demo program #-teken is commentaar
@include "Rekenmachine_II.wasm" # Deze regel altijd toevoegen 

.code MyCode : REKENMACHINE_II 
LOADI $1, 0x00FF  	# Laad 3000hex in register 1
LOADI $2, 0xFF00  	# Laad 2000hex in register 2
XOR $1, $1, $2	
XOR $2, $1, $2
XOR $1, $1, $2	

